// Renderer entry point — canvas, timer logic, and React UI will be added in later steps.
// For step 1 this file is intentionally minimal; the HTML page handles the "boro" display.

console.log('[boro] renderer ready')
